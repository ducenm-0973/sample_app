<li>
  <%= gravatar_for user, size: Settings.all_user_size %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
    <%= link_to t("users.user.delete"), user, method: :delete,
      data: { confirm: t("users.user.sure?") } %>
  <% end %>
</li>
